<filters>
  <filter>fuck this server</filter>
  
  <filter>this server is bad</filter>
  
  <filter>fuck admin</filter>
  
  <filter>shit server</filter>
  
  <filter>rl</filter>
  
  <filter>reallife</filter>
  
  <!-- RL SERVER LISTE -->
  
  <filter>vio</filter>
	
  <filter>v_io</filter>
  
  <filter>vi_o</filter>

  <filter>coa</filter>
  
  <filter>co_a</filter>
  
  <filter>_coa</filter>

 <filter>exo</filter>

 <filter>echso</filter>
 
 <filter>e_xo</filter>


 <filter>skyline</filter>

 
 <filter>xTream</filter>
 
 <filter>xtm</filter>
 
 <filter>xTr_eam</filter>

 
 <filter>tydox</filter>
 
 <filter>ty_dox</filter>

  <filter>nova</filter>
  <filter>kommt alle auf</filter>
  <filter>kommt alle</filter>
  <filter>kommt auf</filter>
  <filter>.com</filter> 
  <filter>.de</filter> 
  <filter>.sa</filter> 
  <filter>.ts3</filter> 
  <filter>reallife-sa.de</filter> 
  <filter>Lakeside</filter> 
  <filter>LakeZ</filter> 
  <filter>serverwerbung</filter> 

  
  

</filters>